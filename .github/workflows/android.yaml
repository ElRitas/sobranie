name: Android CI

on:
  push:
    branches:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout sources
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Gradle cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Run Detekt (Cyclomatic complexity)
        run: cd src && ./gradlew detekt --stacktrace

      - name: Run Ktlint (Code style)
        run: cd src && ./gradlew ktlintCheck --stacktrace

      - name: Unit Tests
        run: cd src && ./gradlew test

      - name: Integration Tests
        run: cd src && ./gradlew integrationTest
